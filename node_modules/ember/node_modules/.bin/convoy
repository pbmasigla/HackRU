#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../convoy/bin/convoy" "$@"
  ret=$?
else 
  node  "$basedir/../convoy/bin/convoy" "$@"
  ret=$?
fi
exit $ret
